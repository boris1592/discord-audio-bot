{
  "tasks": {
    "dev": "deno run --watch --allow-env --allow-read --allow-ffi --allow-net --allow-run main.ts",
    // --all flag is not used because 'node:stream' doesn't type check for some reason
    "check": "deno check $(find . -name '*.ts')"
  }
}
